<div class ="contents row">
     <p><%= @question.text %> </p>
     
      <%= form_for @answer, url:question_answers_path(params[:id]) do |f| %>    

        <%= f.hidden_field :question_id %>
      <%= f.collection_radio_buttons(:samurai_id, Samurai.all, :id, :name) do |b| %>
        <%= b.label {b.radio_button + b.text} %>
      <% end %>
      <%= f.submit "回答する" %>
      
      <% end %>
      
</div>